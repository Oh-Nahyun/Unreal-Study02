#pragma once	// 한번만 읽으라는 의미

#include <stdio.h>

/// 헤더 파일에 들어가는 것 (선언)
/// - 전역 변수
/// - 구조체, 공용체, 열거형
/// - 함수의 원형
/// - 일부 특정한 함수 (인라인 함수)
/// - 매크로

/// 문장 비교 함수
char compare(char* str1, char* str2);
